# Zendesk API Configuration
ZENDESK_SUBDOMAIN=dequehelp
ZENDESK_EMAIL=your-email@example.com
ZENDESK_API_TOKEN=your-api-token-here

# Salesforce API Configuration (Client Credentials Flow)
SF_CLIENT_ID=your-connected-app-client-id
SF_CLIENT_SECRET=your-connected-app-client-secret
SF_LOGIN_URL=https://deque--preetyland.sandbox.my.salesforce.com
# For production: SF_LOGIN_URL=https://login.salesforce.com

# Server Port (optional - Cloud Run uses 8080)
PORT=3001

# Sync Schedule (cron format, default: 2 AM daily)
# Examples:
#   0 2 * * *     = Every day at 2 AM
#   0 */6 * * *   = Every 6 hours
#   0 2 * * 1-5   = Weekdays at 2 AM
SYNC_SCHEDULE=0 2 * * *

# Google OAuth Configuration (for Deque employee login)
# Create credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3001/api/auth/google/callback
# For production: GOOGLE_CALLBACK_URL=https://YOUR-CLOUD-RUN-URL/api/auth/google/callback
ALLOWED_DOMAIN=deque.com

# Session Configuration
SESSION_SECRET=your-random-session-secret-here

# Frontend URL (for OAuth redirects and CORS)
FRONTEND_URL=http://localhost:5173
# For production: FRONTEND_URL=https://your-project.web.app

# Node Environment
# NODE_ENV=development
# For production: NODE_ENV=production
